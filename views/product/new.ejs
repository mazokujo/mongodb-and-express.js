<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New product</title>
</head>

<body>
    <h1>Add a product</h1>
    <form action="/product" method="POST">
        <label for="name">Product's name</label>
        <input type="text" id="name" name="name" placeholder="Product's name">
        <label for="price">Price(unit)</label>
        <input type="number" id="price" name="price" placeholder="Product's price" step=".01">
        <label for="category">Category</label>
        <select name="category" id="category">
            <% for(let category of categories){ %>
                <option value="<%=category%>">
                    <%=category%>
                </option>

                <% } %>

        </select>
        <button>Add your new product</button>
    </form>

    <a href="/product">Back to All product</a>
</body>

</html>